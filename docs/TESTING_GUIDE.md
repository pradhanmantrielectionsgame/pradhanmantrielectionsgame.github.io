# Testing Guide

## Manual Testing
- Use debug commands in the console (see `.github/copilot-instructions.md`)
- Test on both desktop and mobile/touch devices
- Validate phase progression, state sync, and dual-player input

## Automated/Scripted Testing
- (Add scripts or describe process if/when available)

## Bug Reporting
- Note steps to reproduce, expected vs actual behavior
- Reference affected modules and data files

## Debug Commands
- `quickStartGame()`
- `testSkipPhase(5)`
- `getGameStatus()`
- `debugPhaseSystem()`
- `getStatesData()`
- `getGameConfig()`
- `getPlayerData('player1')`
